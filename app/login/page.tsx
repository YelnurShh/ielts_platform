"use client";
import {useState} from "react";
import {useRouter} from "next/navigation";
import {GraduationCap} from "lucide-react";
import {setProfile} from "../../lib/store";
export default function Login(){const router=useRouter();const [role,setRole]=useState<'student'|'teacher'>('student');const [name,setName]=useState('Aruzhan');function login(){setProfile({name:name||'Student',role});router.push(role==='teacher'?'/teacher':'/dashboard')}return <div className="container-wide py-16 min-h-[70vh] grid place-items-center"><div className="card p-7 w-full max-w-md"><div className="h-12 w-12 rounded-2xl bg-[#111827] text-white grid place-items-center"><GraduationCap size={24}/></div><h1 className="text-3xl font-black mt-5">Welcome back</h1><p className="text-[#667085] mt-2">Demo authentication — кейін real auth provider-мен ауыстыруға болады.</p><label className="block text-sm font-bold mt-7">Name<input value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border border-[#e6e8ee] px-4 py-3 outline-none focus:border-[#4f46e5]"/></label><div className="grid grid-cols-2 gap-2 mt-5">{(['student','teacher'] as const).map(r=><button key={r} onClick={()=>setRole(r)} className={`px-4 py-3 rounded-xl border font-bold capitalize ${role===r?'border-[#4f46e5] bg-[#eef2ff] text-[#3730a3]':'border-[#e6e8ee] bg-white'}`}>{r}</button>)}</div><button onClick={login} className="mt-5 w-full rounded-xl bg-[#111827] text-white py-3.5 font-bold">Sign in</button></div></div>}
